# Document Router Settings
# This file allows users to customize the system behavior without touching secret keys.

# ── Tier 2: Keyword Scanning ──────────────────────────────────────────
# Number of pages to scan for keywords. Default: 3
KEYWORD_SCAN_MAX_PAGES = 3

# ── Tier 3: LLM Classification ────────────────────────────────────────
# Number of pages to extract for LLM context. Default: 3
LLM_SCAN_MAX_PAGES = 3

# Max characters to send to LLM (prevents 'Request Too Large' errors). Default: 4000
LLM_MAX_TEXT_CHARS = 4000

# Creativity of the AI (0.0 = deterministic/logical, 0.7 = creative). Default: 0.0
LLM_TEMPERATURE = 0.0

# Max length of the AI's response (keep low for classification). Default: 150
LLM_MAX_TOKENS = 150

# ── Accuracy & Confidence ─────────────────────────────────────────────
# Confidence assigned when a filename matches (0.0 to 1.0). Default: 0.7
CONFIDENCE_FILENAME = 0.7

# Confidence assigned when keywords match (0.0 to 1.0). Default: 0.9
CONFIDENCE_KEYWORD = 0.9

# ── Performance ───────────────────────────────────────────────────────
# Seconds to wait for a pipeline response before timing out. Default: 30
PIPELINE_TIMEOUT = 30

# ── PDF Trait Detection ───────────────────────────────────────────────
# Whether to analyse PDFs with PyMuPDF for traits (Scanned, Handwritten, etc.)
# Set to false to skip this step and save processing time. Default: true
ENABLE_PDF_TRAITS = true

# ── Docling Output Formats ────────────────────────────────────────────
# Which formats to save after Docling processes a PDF.
# Set to true/false. New uploads will produce files in enabled formats.
DOCLING_SAVE_MD   = true
DOCLING_SAVE_JSON = true
DOCLING_SAVE_HTML = false

